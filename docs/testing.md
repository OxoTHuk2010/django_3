# Тестирование

## Цель

Тесты должны проверять не только наличие страниц или endpoints, но и бизнес-правила интернет-магазина: остатки, цены, права доступа, отзывы, создание заказов и оплату.

## Команды

Запуск всех тестов:

```bash
poetry run pytest
```

Поиск тестов:

```bash
poetry run pytest --collect-only -q
```

Django system check:

```bash
poetry run python manage.py check
```

Линтер:

```bash
poetry run ruff check .
```

Проверка миграций выполняется в Docker Compose после пересборки контейнера:

```bash
docker compose up -d --build
docker compose exec web python manage.py makemigrations --check --dry-run
docker compose exec web python manage.py migrate
```

## Уровни тестирования

1. Тесты моделей.
2. Тесты сервисов.
3. Тесты views.
4. Тесты API.

Приоритет — модели и сервисы, потому что именно там должны находиться бизнес-правила.

## Модели

### users

- Пользователь создаётся с уникальным `username`.
- `USERNAME_FIELD` равен `username`.
- `email` необязателен.
- Если `email` указан, дублирующийся email отклоняется.
- `str(user)` возвращает понятное представление пользователя.

### catalog.Category

- `slug` уникален.
- `str(category)` возвращает название.
- Неактивные категории исключаются в selectors.

### catalog.Product

- `slug` уникален.
- `sku` уникален.
- Отрицательная цена отклоняется.
- Остаток не может быть отрицательным.
- Товар с нулевым остатком недоступен.
- Неактивный товар недоступен.

### orders.Order

- Заказ принадлежит пользователю.
- Статус по умолчанию корректен.
- Итоговая сумма не может быть отрицательной.
- Пересчёт суммы заказа учитывает все позиции.

### orders.OrderItem

- Позиция связана с заказом и товаром.
- Цена сохраняется как snapshot.
- Название товара сохраняется как snapshot.
- Количество должно быть положительным.

### reviews.Review

- Пара `user + product` уникальна.
- Рейтинг ниже 1 отклоняется.
- Рейтинг выше 5 отклоняется.
- Опубликованные отзывы можно фильтровать отдельно от скрытых.

### payments.Payment

- Платёж связан с заказом.
- Сумма не может быть отрицательной.
- Финальные статусы определяются корректно.

## Сервисы

### cart

- Добавление товара в корзину.
- Изменение количества.
- Удаление товара.
- Запрет количества больше остатка.
- Корректный расчёт итоговой суммы.

### orders

- Создание заказа из корзины.
- Создание позиций заказа.
- Уменьшение остатков.
- Очистка корзины.
- Запрет заказа сверх остатка.
- Snapshot цены и названия товара.

### reviews

- Нельзя оставить отзыв без покупки, если правило включено.
- Нельзя оставить второй отзыв на тот же товар.
- Рейтинг ограничен диапазоном 1..5.

## API

- `GET /api/products/` доступен всем.
- Создание заказа требует JWT.
- Пользователь видит только свои заказы.
- Чужой заказ недоступен.
- Создание отзыва требует JWT.

## Минимальный Definition of Done для фичи

Фича считается готовой, если есть:

- код;
- миграции, если изменилась модель;
- тесты ключевых бизнес-правил;
- обновлённая документация, если изменилось поведение;
- зелёные `check`, `ruff`, `pytest`.
