# Конфликты и решения

Этот файл фиксирует текущие архитектурные противоречия и вопросы, которые нельзя прятать внутри README или базовой документации. Решённые конфликты остаются здесь как журнал принятых решений, если они влияли на roadmap.

Текущее состояние: конфликты `C024`-`C039` закрыты принятыми ADR и больше не блокируют текущую реализацию. Этапы современной админки, аналитики и payment emulator уже реализованы по ADR `0032`, `0037` и `0033`; перед реализацией REST API compatibility, GraphQL и production runtime нужно сверяться с ADR `0034`-`0035` и актуальным roadmap.

## C001. Корзина: гибридный подход session + database-backed

Статус: решён.

Связанный ADR: `docs/decisions/0002-session-cart.md`.

### Принятое решение

ADR 0002 принимает комбинированный подход:

- неавторизованный пользователь хранит корзину в Django session;
- авторизованный пользователь хранит корзину в БД через `Cart` и `CartItem`;
- после логина session-cart объединяется с DB-корзиной пользователя;
- корзина хранит только `product_id` и `quantity`, а цена, активность и остатки всегда берутся из актуального `Product`;
- корзина не резервирует товар;
- финальная проверка остатков выполняется при checkout внутри транзакции.

### Что изменилось по конфликту

Предыдущий конфликт между `session cart` и `database-backed cart` закрыт: это больше не взаимоисключающие варианты, а две части одной архитектуры.

### Что остаётся сделать в реализации

- Реализовать session-cart для гостя.
- Реализовать сервис работы с DB-корзиной авторизованного пользователя.
- Реализовать merge session-cart в DB-cart после успешного логина.
- Ограничить итоговое количество при merge по `stock_quantity` и системному лимиту.
- Покрыть правила корзины тестами.

## C002. Soft delete в базовой модели

Статус: решён.

Связанный ADR: `docs/decisions/0006-soft-delete.md`.

### Факт по текущему коду

В `common` есть отдельный `SoftDeleteModel`. `TimeStampedModel` не содержит `is_deleted` и `deleted_at`.

По текущему коду `SoftDeleteModel` используется только в `catalog`:

- `catalog.Category`;
- `catalog.Product`.

### Принятое решение

ADR 0006 принимает ограниченное использование soft delete:

- не добавлять soft delete в универсальный `TimeStampedModel`;
- держать soft delete отдельным mixin/abstract-классом;
- использовать `SoftDeleteModel` только для `Product` и `Category`;
- не подключать soft delete к заказам, оплатам, позициям заказа и другим исторически значимым сущностям;
- пересматривать ADR 0006, если soft delete понадобится другим моделям.

### Что остаётся сделать в реализации

- В selectors каталога всегда исключать `is_deleted=True`.
- Покрыть публичные выборки каталога тестами.

## C003. Основной логин пользователя

Статус: решён.

Связанный ADR: `docs/decisions/0007-username-user-login.md`.

### Факт по текущему коду

`users.User` наследует `AbstractUser` и использует стандартный логин Django:

```text
USERNAME_FIELD = "username"
REQUIRED_FIELDS = []
```

Поле `email` является вспомогательным контактным полем:

```text
blank=True
null=True
unique=True
```

### Принятое решение

ADR 0007 фиксирует:

- основной логин пользователя — `username`;
- `email` не участвует в основной аутентификации;
- стандартные сценарии Django Admin, `createsuperuser` и JWT token obtain используют `username`;
- переход к login by email возможен только через отдельный ADR и изменение логики аутентификации.

Конфликт закрыт. Текущая модель и миграции соответствуют ADR 0007.

## C004. Несколько платежей на один заказ или один платёж на заказ

Статус: решён.

Связанный ADR: `docs/decisions/0008-payment-order.md`.

### Факт по текущему коду

`payments.Payment.order` сейчас реализован как `ForeignKey` на `Order`. Это означает связь `Order 1 -> N Payment`.

### Принятое решение

ADR 0008 фиксирует:

- один заказ может иметь несколько `Payment`-записей;
- новые попытки оплаты создают новые записи, старые платежи не перезаписываются;
- неуспешные, отменённые и возвращённые платежи остаются в истории;
- для MVP checkout должен ограничивать успешную оплату правилом: у заказа не должно быть больше одного актуального успешного платежа;
- повторная успешная оплата уже оплаченного заказа должна быть запрещена бизнес-логикой.

### Что остаётся сделать в реализации

- Реализовать правило запрета повторной успешной оплаты в checkout/payment service.
- Покрыть сценарии оплаты тестами.

## C005. Связь пользователя и отзыва

Статус: решён.

### Факт по текущему коду

`reviews.Review.user` реализован как `ForeignKey` на пользователя.

В модели также есть `UniqueConstraint(fields=["user", "product"])`.

### Принятое решение

Текущая модель соответствует бизнес-правилу:

- пользователь может оставить отзывы к разным товарам;
- пользователь не может оставить второй отзыв на тот же товар.

Конфликт закрыт. Текущая модель и миграции соответствуют бизнес-правилу отзывов.

## C006. URL детальной страницы товара

Статус: решён.

Связанный этап: этап 7 — страница товара.

### Факт по текущей документации

В `docs/roadmap.md` для этапа 7 зафиксирован маршрут:

```text
/products/<slug>/
```

Маршрут списка товаров уже использует тот же namespace:

```text
/products/
```

### Принятое решение

Канонический web URL детальной страницы товара:

```text
/products/<slug>/
```

Причина: детальная страница остаётся внутри общего namespace каталога `/products/`, а публичные ссылки списка и карточки товара выглядят последовательно.

### Что остаётся сделать в реализации

- Добавить URL `/products/<slug>/` в `catalog.urls`.
- Реализовать `ProductDetailView`.
- Добавить selector получения одного публичного товара по `slug`.
- Покрыть доступность активного товара и недоступность неактивного/soft-deleted товара тестами.

## C007. Источник изображения на детальной странице товара

Статус: решён.

Связанный этап: этап 7 — страница товара.

Связанный ADR: `docs/decisions/0009-img-source.md`.

### Факт по текущему коду

В модели каталога уже есть `ProductImage` с полями:

- `product`;
- `image`;
- `alt_text`;
- `is_main`;
- `sort_order`;
- `created_at`;
- `updated_at`.

В `src/prepare/` есть подготовленные UI-ассеты и изображения, но эта директория не должна становиться runtime-зависимостью рабочей реализации этапа 7.

### Принятое решение

ADR 0009 фиксирует:

- источником изображения товара на детальной странице является только `ProductImage`;
- обязательный fallback-placeholder на этапе 7 не добавляется;
- если у товара нет связанных `ProductImage`, страница должна корректно отображаться без изображения;
- допустимо не показывать блок изображения или показать текстовое состояние `Изображение отсутствует`;
- запрещено подбирать изображение по `slug`;
- запрещено напрямую использовать файлы из `src/prepare/` в рабочем шаблоне;
- запрещено зашивать пути к изображениям во view или template вне модели `ProductImage`;
- основное изображение определяется через `ProductImage.is_main`.

### Что изменилось по конфликту

Предыдущий конфликт между `ProductImage`, placeholder и прямым использованием `src/prepare/` закрыт.

На этапе 7 не нужно добавлять placeholder и не нужно переносить изображения из `src/prepare/` в runtime-статику. Отсутствие изображения является допустимым состоянием интерфейса.

### Что остаётся сделать в реализации

- В selector/view детальной страницы получать изображения через связь `ProductImage`.
- В шаблоне корректно обработать товар без изображений.
- Не использовать `src/prepare/` напрямую.
- Добавить тесты для товара с изображением и без изображения.

## C008. Кнопка покупки на странице товара до реализации корзины

Статус: решён.

Связанный этап: этап 7 — страница товара.

Связанный ADR: `docs/decisions/0010-button.md`.

### Факт по текущему roadmap

Этап 7 реализует детальную страницу товара, но корзина реализуется только на этапе 8.

Поэтому на этапе 7 нельзя добавлять активную POST-форму покупки без backend-логики корзины.

### Принятое решение

ADR 0010 фиксирует:

- для товара с `stock_quantity > 0` показывать disabled-кнопку `Добавить в корзину`;
- рядом или под кнопкой показывать пояснение, что корзина будет доступна позже;
- для товара с `stock_quantity = 0` показывать состояние `Нет в наличии`;
- не выполнять POST-запросы на добавление товара в корзину;
- не создавать временную frontend-корзину;
- активную форму добавления товара в корзину реализовывать только на этапе 8.

### Что изменилось по конфликту

Предыдущий конфликт между скрытой кнопкой, disabled-кнопкой и временной frontend-симуляцией закрыт.

На этапе 7 показываем будущую точку действия, но не реализуем покупку.

### Что остаётся сделать в реализации

- Добавить disabled-кнопку в шаблон детальной страницы.
- Добавить пояснение рядом с кнопкой.
- Для `stock_quantity = 0` показать `Нет в наличии`.
- Покрыть disabled-состояние тестами.
- Проверить, что на этапе 7 нет активной POST-формы добавления в корзину.


## C009. Отзывы и рейтинг на странице товара до реализации review service

Статус: решён.

Связанный этап: этап 7 — страница товара.

Связанный ADR: `docs/decisions/0011-reviews-rating.md`.

### Факт по текущему коду

Модель `Review` уже есть и содержит поле `status`.

Публичный статус отзыва:

```text
Review.Status.PUBLISHED
```

Полноценный review service, форма создания отзыва и проверка покупки относятся к более позднему этапу.

### Принятое решение

ADR 0011 фиксирует:

- на этапе 7 показывать только read-only опубликованные отзывы;
- средний рейтинг считать только по опубликованным отзывам;
- количество отзывов считать только по опубликованным отзывам;
- форму создания отзыва на этапе 7 не добавлять;
- не показывать отзывы в статусах `draft`, `pending`, `rejected`, `hidden` и любых будущих непубличных статусах;
- состояние без отзывов должно отображаться корректно.

### Что изменилось по конфликту

Предыдущий конфликт между read-only отзывами, полным скрытием отзывов и преждевременной формой создания закрыт.

На этапе 7 отзывы используются только на чтение.

### Что остаётся сделать в реализации

- В selector/view детальной страницы получить отзывы по `status=Review.Status.PUBLISHED`.
- Посчитать средний рейтинг по опубликованным отзывам.
- Посчитать количество опубликованных отзывов.
- Добавить read-only блок отзывов в шаблон.
- Не добавлять форму создания отзыва.
- Покрыть публичные и непубличные отзывы тестами.


## C010. Правило похожих товаров на странице товара

Статус: решён.

Связанный этап: этап 7 — страница товара.

Связанный ADR: `docs/decisions/0012-rule-product.md`.

### Факт по текущему коду

В текущей модели каталога нет тегов, брендов, атрибутов, просмотров и полноценной рекомендательной системы.

Для минимальной выборки уже доступны:

- `Product.category`;
- `Product.is_active`;
- `Product.is_deleted`;
- `Product.stock_quantity`;
- `Category.is_active`.

### Принятое решение

ADR 0012 фиксирует:

- похожие товары выбираются только из той же категории;
- текущий товар исключается;
- в блок попадают только активные неудалённые товары;
- категория похожих товаров должна быть активной;
- максимум отображается 3 товара;
- товары с `stock_quantity = 0` можно показывать, но с состоянием `Нет в наличии`;
- подбор по цене, тегам, брендам, популярности, заказам и просмотрам на этапе 7 не реализуется.

### Что изменилось по конфликту

Предыдущий конфликт между простой категорией, близкой ценой и рекомендательной логикой закрыт.

На этапе 7 используется минимальное правило похожих товаров по категории.

### Что остаётся сделать в реализации

- Добавить selector похожих товаров.
- Исключить текущий товар из результата.
- Ограничить результат 3 товарами.
- Отфильтровать неактивные и soft-deleted товары.
- Проверять активность категории.
- Покрыть правило похожих товаров тестами.

## C011. Web-маршруты и HTTP-контракт корзины

Статус: решён и реализован.

Связанный этап: этап 8 — корзина.

Связанный ADR: `docs/decisions/0013-cart-web-routes.md`.

### Принятое решение

Для web-интерфейса корзины используются отдельные маршруты действий с передачей `product_id` в URL:

- `GET /cart/` — просмотр корзины;
- `POST /cart/add/<int:product_id>/` — добавление товара;
- `POST /cart/update/<int:product_id>/` — изменение количества;
- `POST /cart/remove/<int:product_id>/` — удаление позиции;
- `POST /cart/clear/` — очистка корзины.

Все операции изменения состояния выполняются только через `POST`. После успешного `POST` выполняется redirect на `/cart/`.

### Состояние реализации

- [x] Добавлен `apps/cart/urls.py`.
- [x] Cart-маршруты подключены в `config/urls.py`.
- [x] Реализованы thin views для просмотра и POST-действий корзины.
- [x] Disabled-кнопка на странице товара заменена POST-формой добавления в корзину.
- [x] Маршрутные сценарии покрыты web-тестами.

## C012. Сервисный слой корзины и граница ответственности view

Статус: решён и реализован.

Связанный этап: этап 8 — корзина.

Связанный ADR: `docs/decisions/0014-cart-service-layer.md`.

### Принятое решение

Бизнес-логика корзины размещается в `apps/cart/services.py`.

Публичный контракт этапа 8:

- `get_cart_snapshot(request)`;
- `add_to_cart(request, product, quantity)`;
- `update_cart_item(request, product, quantity)`;
- `remove_from_cart(request, product)`;
- `clear_cart(request)`;
- `merge_session_cart_to_user_cart(request, user)`.

View остаются HTTP-слоем: принимают request, читают форму, находят товар, вызывают сервис, ставят messages и выполняют render или redirect.

### Состояние реализации

- [x] Создан `apps/cart/services.py`.
- [x] Реализованы публичные сервисные функции.
- [x] View не знают внутренний формат session-cart.
- [x] Нормализация, totals и выбор storage находятся в сервисном слое.
- [x] Сервисный слой покрыт тестами бизнес-правил корзины.

## C013. Когда выполнять merge session-cart в DB-cart

Статус: решён и реализован частично согласно ADR.

Связанный этап: этап 8 — корзина.

Связанный ADR: `docs/decisions/0015-cart-merge-timing.md`.

### Принятое решение

На этапе 8 был реализован и протестирован `merge_session_cart_to_user_cart(request, user)`, но функция не подключалась к `user_logged_in` signal.

На объединённом этапе 9-10 merge подключён явно в пользовательском login-flow: после успешного входа и после регистрации вызывается `merge_session_cart_to_user_cart(request, user)`.

### Состояние реализации

- [x] Merge-сервис реализован.
- [x] Объединение session-cart и DB-cart покрыто тестами.
- [x] Merge не подключён к `user_logged_in` signal.
- [x] Merge явно вызывается из `UserLoginView.form_valid()`.
- [x] Merge явно вызывается из `UserRegisterView.form_valid()`.
- [x] Вход с гостевой корзиной покрыт web-тестом.

## C014. Политика количества товара: add, update и merge

Статус: решён и реализован.

Связанный этап: этап 8 — корзина.

Связанный ADR: `docs/decisions/0016-cart-quantity-policy.md`.

### Принятое решение

Вводится единая политика количества:

- `add_to_cart` увеличивает количество существующей позиции;
- `update_cart_item` заменяет количество позиции;
- `remove_from_cart` удаляет позицию;
- `clear_cart` очищает корзину;
- системный максимум одной позиции: `MAX_CART_ITEM_QUANTITY = 99`.

Для пользовательских операций превышение остатка или системного лимита является ошибкой и не меняет корзину.

Для merge используется безопасное ограничение: `min(db_quantity + session_quantity, product.stock_quantity, MAX_CART_ITEM_QUANTITY)`.

### Состояние реализации

- [x] Добавлен `MAX_CART_ITEM_QUANTITY = 99`.
- [x] Количество проверяется внутри сервисов.
- [x] Корзина не меняется при ошибке пользовательской операции.
- [x] `add`, `update`, превышение остатка, превышение лимита и merge покрыты тестами.

## C015. Недоступные товары и битые product_id в session-cart

Статус: решён и реализован.

Связанный этап: этап 8 — корзина.

Связанный ADR: `docs/decisions/0017-session-cart-invalid-products.md`.

### Принятое решение

Нормализация корзины выполняется в service-layer при построении snapshot.

Из корзины автоматически удаляются позиции, если товар:

- не найден по `product_id`;
- soft-deleted;
- имеет `is_active=False`;
- находится в неактивной или скрытой категории.

Товары с `stock_quantity = 0` остаются видимыми, но помечаются как недоступные для оформления. Если `quantity` больше текущего остатка, количество не обрезается silently при просмотре корзины: позиция остаётся с предупреждением, а checkout должен быть недоступен до явного изменения количества.

### Состояние реализации

- [x] Нормализация snapshot реализована в `apps/cart/services.py`.
- [x] Очистка битых и скрытых позиций сохраняется обратно в session.
- [x] Snapshot возвращает warnings для шаблона.
- [x] Snapshot отражает `has_unavailable_items` и `can_checkout`.
- [x] Edge cases покрыты сервисными тестами.

## C016. Объединение этапов 9 и 10: checkout, заказы и личный кабинет

Статус: решён и реализован, отдельный ADR не требуется.

Связанные этапы:

- этап 9 — checkout и заказы;
- этап 10 — пользователи и личный кабинет.

### Факт по текущему коду и плану

В проекте уже есть:

- обязательная связь `Order.user`;
- гибридная корзина: session для гостя и БД для авторизованного пользователя;
- сервис merge session-cart в DB-cart;
- бизнес-правило: авторизованный пользователь может оформлять заказ и смотреть свои заказы;
- реализованный личный кабинет, где есть вход, выход, профиль и история заказов.

### Принятое рабочее решение

Этапы 9 и 10 реализуются как один вертикальный срез.

Checkout на текущем MVP доступен только авторизованному пользователю. Гостевой checkout не реализуется, потому что текущая модель `Order` требует `user` и история заказов должна быть привязана к аккаунту.

Поведение:

- гость может собирать корзину в session;
- при переходе к checkout гость должен перейти к входу или регистрации;
- после успешного входа session-cart объединяется с DB-корзиной пользователя;
- заказ создаётся только для авторизованного пользователя;
- пользователь видит только свои заказы;
- `Order.user` не делается nullable;
- отдельная модель гостевого заказа не добавляется.

### Почему отдельный ADR не нужен

Решение следует из уже принятой структуры проекта и текущих моделей:

- `Order.user` обязателен;
- web-аутентификация использует Django session;
- личный кабинет нужен для истории заказов;
- гостевой checkout не был обязательной частью MVP.

Отдельный ADR понадобится только если позже будет принято решение разрешить гостевые заказы или отделить заказ от пользователя.

## C017. Контракт checkout-формы и данные доставки

Статус: решён и реализован, отдельный ADR не требуется.

Связанные этапы:

- этап 9 — checkout и заказы;
- этап 10 — пользователи и личный кабинет.

### Факт по текущему коду

Модель `Order` уже хранит snapshot данных покупателя:

- `customer_name`;
- `customer_email`;
- `customer_phone`;
- `delivery_address`;
- `comment`.

Отдельной модели адресов доставки пока нет.

### Принятое рабочее решение

Checkout-форма использует только поля, которые уже есть в `Order`.

Обязательные поля:

- `customer_name`;
- `customer_email`;
- `customer_phone`;
- `delivery_address`.

Необязательное поле:

- `comment`.

Допустимо предзаполнять `customer_name` и `customer_email` из текущего пользователя, если эти данные доступны. Пользователь всё равно подтверждает snapshot-данные в форме checkout.

На объединённом этапе 9-10 не добавляются:

- отдельная модель адресов доставки;
- адресная книга пользователя;
- сложный профиль доставки;
- выбор сохранённого адреса.

### Почему отдельный ADR не нужен

Это не меняет архитектуру и не требует миграций: контракт формы напрямую следует из уже существующей модели `Order`.

Отдельный ADR понадобится, если появится решение хранить несколько адресов доставки или переиспользовать адреса между заказами.

## C018. Граница ответственности между cart service и orders service

Статус: решён и реализован, отдельный ADR не требуется.

Связанные этапы:

- этап 9 — checkout и заказы;
- этап 10 — пользователи и личный кабинет.

### Факт по текущему коду

Сервис корзины уже предоставляет:

- `get_cart_snapshot(request)`;
- `clear_cart(request)`;
- `CartSnapshot.can_checkout`;
- нормализацию недоступных позиций;
- единые правила расчёта количества и стоимости.

### Принятое рабочее решение

`orders.services` не должен читать session напрямую и не должен дублировать правила корзины.

Граница ответственности:

- `cart.services` отвечает за чтение, нормализацию и очистку корзины;
- `orders.services` отвечает за атомарное создание заказа, позиций заказа, применение результата `apps.payment_emulator`, уменьшение остатков при успешной оплате и создание платежа;
- view остаётся HTTP-слоем: проверяет авторизацию, валидирует форму, получает snapshot корзины, вызывает сервис заказа, ставит messages и делает redirect.

Публичный контракт для этапа 9-10:

```python
create_order_from_cart(
    *,
    user,
    cart_snapshot,
    shipping_data,
)
```

`orders.services` принимает `CartSnapshot`, но внутри транзакции повторно блокирует и перечитывает товары через `select_for_update()`. Snapshot используется как пользовательски подтверждённый состав корзины, а не как единственный источник складских остатков.

Очистка корзины выполняется только после успешного создания заказа.

### Почему отдельный ADR не нужен

Это уточнение уже принятого ADR 0004 и ADR 0014:

- заказ создаётся в service-layer;
- корзина имеет отдельный service-layer;
- checkout не должен читать внутренний формат session-cart.

## C019. Платёж и статусы после checkout

Статус: решён и реализован, отдельный ADR не требуется.

Связанные документы:

- `docs/decisions/0008-payment-order.md`;
- `docs/decisions/0004-order-transaction.md`;
- `docs/decisions/0033-payment-emulator.md`.

### Факт по текущему коду

В `Order` есть статусы:

- `new`;
- `paid`;
- `processing`;
- `shipped`;
- `completed`;
- `cancelled`.

В `Payment` есть статусы:

- `pending`;
- `succeeded`;
- `failed`;
- `cancelled`;
- `refunded`.

ADR 0008 уже разрешает несколько платежей на один заказ. ADR 0033 заменил первичную mock-оплату на weighted payment emulator.

### Принятое рабочее решение

Checkout создаёт платёж через `apps.payment_emulator`.

При `succeeded`:

- создаётся `Order`;
- создаются `OrderItem`;
- уменьшаются остатки товаров;
- создаётся `Payment` со статусом `succeeded`;
- заказ получает статус `paid`;
- для платежа заполняется `provider = "payment_emulator"`;
- сумма платежа равна `Order.total_price`;
- корзина очищается.

При `failed`, `cancelled` или `pending`:

- создаётся `Order` в статусе `new`;
- создаются `OrderItem`;
- создаётся `Payment` с соответствующим статусом;
- остатки не уменьшаются;
- корзина сохраняется.

Запрет повторной успешной оплаты остаётся бизнес-правилом payment/checkout service. В рамках первичного checkout повторная успешная оплата не возникает, потому что заказ создаётся один раз из корзины.

### Почему отдельный ADR не нужен

Это конкретизация ADR 0008 и ADR 0033 для MVP. Реальная платёжная интеграция не реализуется, но checkout больше не считает оплату всегда успешной.

## C020. Поведение checkout при изменении остатков во время оформления

Статус: решён и реализован, отдельный ADR не требуется.

Связанные документы:

- `docs/decisions/0004-order-transaction.md`;
- `docs/decisions/0017-session-cart-invalid-products.md`.

### Факт по текущему коду

Корзина уже умеет показывать `can_checkout=False`, если товар недоступен или количество превышает остаток.

ADR 0004 требует:

- `transaction.atomic()`;
- `select_for_update()`;
- повторную проверку остатков при checkout.

### Принятое рабочее решение

Checkout работает по правилу all-or-nothing.

Если во время оформления заказа остатки изменились:

- заказ не создаётся;
- позиции заказа не создаются;
- остатки не уменьшаются;
- платёж не создаётся;
- корзина не очищается;
- пользователь возвращается к корзине или checkout-форме с понятной ошибкой.

Checkout не должен автоматически уменьшать количество товара и не должен создавать частичный заказ по оставшимся позициям. Пользователь должен явно подтвердить новый состав корзины.

Для снижения риска deadlock товары внутри транзакции блокируются в стабильном порядке, например по `Product.id`.

### Почему отдельный ADR не нужен

Это прямое следствие ADR 0004 и ADR 0017:

- создание заказа атомарно;
- корзина не резервирует товар;
- финальная проверка остатков выполняется при checkout;
- пользователь не должен получить заказ с неожиданно изменённым составом.

## C021. Какой заказ даёт право оставить отзыв

Статус: решён.

Связанный этап: этап 11 — отзывы.

Связанный ADR: `docs/decisions/0021-review-eligible-order-status.md`.

### Факт по текущему коду

В проекте уже есть:

- `Review.user`;
- `Review.product`;
- `Review.is_verified_purchase`;
- уникальность пары `user + product`;
- статусы заказа `new`, `paid`, `processing`, `shipped`, `completed`, `cancelled`;
- checkout создаёт заказ со статусом `paid`;
- `OrderItem` связывает заказ с купленным товаром.

### Принятое решение

Подтверждённой покупкой для права оставить отзыв считается заказ текущего пользователя, который содержит нужный товар через `OrderItem` и находится в одном из статусов:

- `paid`;
- `processing`;
- `shipped`;
- `completed`.

Статусы `new` и `cancelled` не дают право оставить отзыв.

На этапе 11 `reviews.services` не проверяет `Payment` напрямую: статус `Order` считается агрегированным бизнес-состоянием заказа.

`Review.is_verified_purchase=True` выставляется при создании отзыва, если `user_can_review_product(user, product)` подтвердил покупку через `Order` и `OrderItem`.

### Что изменилось по конфликту

Конфликт закрыт ADR 0021. Реализация этапа 11 должна опираться на это правило и покрыть его service-тестами.

## C022. Где и как пользователь создаёт отзыв в web-интерфейсе

Статус: решён.

Связанный этап: этап 11 — отзывы.

Связанный ADR: `docs/decisions/0022-review-web-create-contract.md`.

### Факт по текущему коду

Детальная страница товара уже показывает read-only блок опубликованных отзывов и рейтинг.

Формы создания отзыва сейчас нет. В `Review` уже есть поля:

- `rating`;
- `title`;
- `text`;
- `status`;
- `is_verified_purchase`.

### Принятое решение

Форма создания отзыва может отображаться на детальной странице товара `/products/<slug>/`, но создание отзыва обрабатывает приложение `reviews`, а не `catalog`.

Канонический POST-маршрут:

```text
POST /reviews/products/<slug>/add/
```

`catalog` отвечает за отображение товара, опубликованных отзывов и формы или уведомления. `reviews` отвечает за HTTP-обработчик создания отзыва, форму, сервисы, проверку права и redirect после обработки.

После успешного создания пользователь возвращается на страницу товара, предпочтительно к блоку отзывов `/products/<slug>/#reviews`.

### Что изменилось по конфликту

Конфликт закрыт ADR 0022. Реализация этапа 11 должна добавить `reviews/forms.py`, `reviews/services.py`, `reviews/views.py`, `reviews/urls.py` и связать форму на странице товара с namespace `reviews`.

## C023. Начальный статус и видимость нового отзыва

Статус: решён текущей моделью и бизнес-правилами, отдельный ADR не требуется.

Связанный этап: этап 11 — отзывы.

### Факт по текущему коду

`Review.status` по умолчанию равен `pending`.

Публичная детальная страница товара показывает только отзывы со статусом `published`.

В админке уже есть actions публикации, отклонения и скрытия отзывов.

### Принятое рабочее решение

Новый отзыв после создания получает статус `pending` и не появляется публично до модерации.

Пользователю после отправки нужно показать сообщение, что отзыв отправлен на модерацию.

### Почему отдельный ADR не нужен

Решение уже следует из модели `Review`, текущего read-only блока отзывов и правил модерации.

## C024. Архитектура REST API и граница ответственности `apps/api`

Статус: решён ADR 0023.

Связанный этап: этап 12 — REST API.

### Факт по текущему коду

API-инфраструктура уже подключена:

- `rest_framework`;
- SimpleJWT;
- `drf_spectacular`;
- `/api/schema/`;
- `/api/docs/`;
- `/api/token/`;
- `/api/token/refresh/`.

`apps/api/urls.py` пока пустой, а доменные `api/serializers.py`, `api/views.py`, `api/permissions.py` внутри приложений ещё не созданы.

В `docs/architecture.md` уже зафиксирован целевой принцип: API-код должен находиться рядом с доменным приложением, а `apps/api` должен только собирать маршруты.

### Конфликт

Нужно определить, как именно строить API-слой:

- `ViewSet` + router;
- `APIView` / generic views;
- смешанный подход;
- единый router в `apps/api`;
- отдельные `urls.py` внутри доменных `api/` модулей с агрегацией в `apps/api/urls.py`.

### Риск без решения

Если начать реализацию без контракта, API-код может оказаться размазан между `apps/api`, доменными приложениями и view-слоем web-интерфейса. Это нарушит уже принятое архитектурное правило и усложнит тестирование.

### Что нужно решить

- Где физически лежат serializers, views, permissions и urls.
- Используются ли DRF routers.
- Как `apps/api/urls.py` собирает доменные маршруты.
- Как именуются API namespaces.
- Какие части API считаются публичным контрактом MVP.

## C025. Контракт Product API: идентификатор, поля, фильтры и пагинация

Статус: решён ADR 0024.

Связанный этап: этап 12 — REST API.

### Факт по текущему коду

В web-каталоге уже реализованы:

- список товаров `/products/`;
- детальная страница `/products/<slug>/`;
- поиск;
- фильтр категории;
- фильтр цены;
- сортировка;
- пагинация;
- публичная видимость только активных неудалённых товаров из активных неудалённых категорий;
- изображения через `ProductImage`;
- read-only опубликованные отзывы и рейтинг.

В `docs/api.md` пока указан план:

```text
GET /api/products/
GET /api/products/<id>/
```

### Конфликт

Нужно определить публичный контракт Product API:

- использовать `id` или `slug` для detail endpoint;
- какие поля отдавать в list и detail;
- отдавать ли изображения, рейтинг, количество отзывов и категорию;
- какие query-параметры поддерживать;
- совпадают ли фильтры API с web-фильтрами;
- какая пагинация используется.

### Риск без решения

Если сделать API detail по `id`, а web живёт по `slug`, появятся два разных публичных способа идентификации товара. Если не зафиксировать поля, тесты и Swagger будут описывать случайный текущий serializer, а не осознанный контракт.

### Что нужно решить

- Canonical lookup для товара в API: `id` или `slug`.
- Набор полей list serializer.
- Набор полей detail serializer.
- Поддерживаемые фильтры и сортировки.
- Формат пагинации.
- Нужно ли включать опубликованные отзывы в product detail или держать отдельный review endpoint.

## C026. Контракт API-корзины и связь с web/session-корзиной

Статус: решён ADR 0025.

Связанный этап: этап 12 — REST API.

### Факт по текущему коду

В web-интерфейсе корзина гибридная:

- гость использует session-cart;
- авторизованный пользователь использует DB-cart;
- после входа session-cart объединяется с DB-cart;
- все операции корзины идут через `apps/cart/services.py`.

В `docs/api.md` пока указан общий пункт:

```text
POST /api/cart/
```

### Конфликт

Нужно определить, как API работает с корзиной:

- API-корзина доступна только по JWT и работает только с DB-cart;
- API поддерживает session-cart для анонимных клиентов;
- API использует те же service-функции, что web;
- API имеет отдельный контракт операций: add, update, remove, clear, detail;
- один endpoint `POST /api/cart/` или несколько явных endpoints.

### Риск без решения

Если API начнёт поддерживать session-cart, появится зависимость от cookies и состояния браузера, что плохо сочетается с JWT. Если сделать только DB-cart, нужно явно зафиксировать, что API-корзина требует авторизации и не повторяет гостевой web-сценарий.

### Что нужно решить

- Требует ли API-корзина JWT всегда.
- Использует ли API session-cart.
- Список API endpoints корзины.
- Формат payload для add/update/remove/clear.
- Формат ответа snapshot корзины.
- Как API сообщает о превышении остатка, неактивном товаре и битых позициях.

## C027. Контракт создания заказа через API

Статус: решён ADR 0026.

Связанный этап: этап 12 — REST API.

### Факт по текущему коду

Web checkout уже реализован:

- заказ создаётся из текущей корзины;
- `orders.services.create_order_from_cart()` принимает `CartSnapshot`;
- создание заказа атомарное;
- товары блокируются через `select_for_update()`;
- создаётся платёж через `apps.payment_emulator`;
- после успешной оплаты корзина очищается.

В `docs/api.md` пока указан план:

```text
POST /api/orders/
```

### Конфликт

Нужно определить, как API создаёт заказ:

- только из текущей API-корзины пользователя;
- из переданного списка позиций;
- с обязательными shipping/contact fields;
- с переиспользованием `create_order_from_cart()`;
- с очисткой корзины после успешной оплаты;
- с тем же payment emulator поведением, что web.

### Риск без решения

Если API позволит передавать позиции напрямую, он может обойти правила корзины и нормализации. Если API создаёт заказ только из корзины, это нужно явно описать, иначе endpoint будет непонятен для клиента и Swagger.

### Что нужно решить

- Источник позиций заказа: текущая корзина или request payload.
- Payload контактных данных и доставки.
- Очищается ли корзина после успешного API checkout.
- Формат ответа после создания заказа.
- Формат ошибки при пустой корзине, нехватке остатков и невалидных данных.
- Использует ли API тот же service-layer, что web.

## C028. Контракт API-регистрации и JWT после регистрации

Статус: решён ADR 0027.

Связанный этап: этап 12 — REST API.

### Факт по текущему коду

ADR 0007 зафиксировал, что основной логин пользователя — `username`. Web-регистрация уже создаёт пользователя, выполняет вход через session и объединяет гостевую корзину.

JWT token endpoint уже доступен:

```text
POST /api/token/
POST /api/token/refresh/
```

В `docs/api.md` пока указан план:

```text
POST /api/users/register/
```

### Конфликт

Нужно определить поведение API-регистрации:

- регистрация только создаёт пользователя;
- регистрация создаёт пользователя и сразу возвращает JWT pair;
- какие поля обязательны: `username`, `password`, `email`;
- допускается ли пустой email;
- выполняется ли merge session-cart после API-регистрации;
- нужен ли отдельный API login endpoint или достаточно SimpleJWT `/api/token/`.

### Риск без решения

Если API-регистрация начнёт вести себя как web-регистрация, появится неявная зависимость от session и merge-cart. Если она сразу возвращает JWT, это нужно явно покрыть тестами и описать в Swagger.

### Что нужно решить

- Payload регистрации.
- Response регистрации.
- Возвращать ли JWT сразу.
- Делать ли API login alias или использовать `/api/token/`.
- Как обрабатывать уникальность username/email.
- Нужна ли связь API-регистрации с корзиной.

## C029. Контракт Review API и связь с уже реализованным web-сценарием

Статус: решён ADR 0028.

Связанный этап: этап 12 — REST API.

### Факт по текущему коду

Этап 11 реализовал web-создание отзыва:

```text
POST /reviews/products/<slug>/add/
```

Право оставить отзыв проверяется через `reviews.services.user_can_review_product()`. Новый отзыв создаётся в статусе `pending` и публично отображается только после модерации.

В `docs/api.md` пока указан план:

```text
POST /api/products/<id>/reviews/
```

### Конфликт

Нужно определить Review API:

- product lookup через `id` или `slug`;
- endpoint только для создания или также для списка отзывов товара;
- показывать только опубликованные отзывы или также собственный pending-отзыв пользователя;
- использовать ли `reviews.services.create_product_review()`;
- какой response возвращать после создания pending-отзыва.

### Риск без решения

Если API будет использовать `id`, а web-сценарий `slug`, появится расхождение контрактов. Если API обойдёт `reviews.services`, правила покупки и уникальности могут начать отличаться от web.

### Что нужно решить

- URL review endpoint.
- Payload создания отзыва.
- Response успешного создания.
- Формат ошибки без покупки, при дубле и при невалидном рейтинге.
- Нужно ли отдавать pending-отзыв автору.
- Как API связан с опубликованными отзывами на product detail.

## C030. Единый формат ошибок и permissions в REST API

Статус: решён ADR 0029.

Связанный этап: этап 12 — REST API.

### Факт по текущему коду

В web-слое ошибки показываются через Django messages и redirects. В API нужны HTTP status codes и JSON-ответы.

В плане этапа 12 есть пункт:

```text
Добавить permissions.
```

Пока не определены:

- формат ошибки;
- names permissions;
- коды ответов для бизнес-ошибок;
- поведение при доступе к чужим заказам;
- поведение при попытке создать заказ или отзыв без прав.

### Конфликт

Нужно определить единый API error/permission contract:

- использовать стандартный DRF error format;
- вводить свой формат `{code, detail, fields}`;
- какие permissions нужны: `IsOwner`, `IsOrderOwner`, `CanReviewProduct`;
- возвращать `403` или `404` для чужого заказа;
- как маппить service-layer exceptions в API responses.

### Риск без решения

Если каждый endpoint будет возвращать ошибки по-своему, API станет трудно тестировать и документировать. Если не решить `403` vs `404` для чужих ресурсов, можно случайно раскрыть существование чужих заказов.

### Что нужно решить

- Базовый JSON-формат ошибок.
- HTTP status codes для auth, permission, validation и business-rule ошибок.
- Набор permissions.
- Нужно ли скрывать чужие объекты через `404`.
- Правила преобразования service exceptions в DRF responses.

## C031. Политика seed-данных и использование `src/prepare/`

Статус: решён ADR 0030.

Связанный этап: этап 18 — seed-данные.

### Факт по текущему коду

`src/prepare/` сохранён как reference/source UI и тестовых данных, находится в `.gitignore` и не должен влиять на runtime проекта. Нужные public assets уже перенесены в tracked `src/static/shop`.

Этапы 18 и 22 реализовали management command:

```text
python manage.py seed_demo_data
```

Команда существует, идемпотентна, создаёт русскоязычные demo-данные, копирует product images из tracked static в `MEDIA_ROOT/demo/products` и не читает `src/prepare` во время runtime/seed.

### Конфликт

Нужно определить поведение seed-команды:

- идемпотентное создание данных или каждый запуск создаёт новые записи;
- нужен ли флаг `--reset`;
- можно ли использовать ассеты из `src/prepare/`;
- какие demo-пользователи и пароли создаются;
- какие статусы заказов, оплат и отзывов создаются;
- как не повредить реальные данные в локальной БД.

### Риск без решения

Seed-команда может случайно дублировать данные, перезаписать пользовательские записи или начать зависеть от директории `src/prepare/` как runtime-источника. Это противоречит текущему решению держать `src/prepare/` вне рабочего приложения.

### Что нужно решить

- Идемпотентность seed-команды.
- Нужен ли `--reset` и какие stop-условия у destructive режима.
- Источник изображений и демо-данных.
- Состав demo-аккаунтов.
- Состав категорий, товаров, заказов, оплат и отзывов.
- Какие данные допустимо создавать в Docker PostgreSQL перед демонстрацией.

## C032. MyShop vs Hop & Barley template

Статус: решён.

Связанный ADR: `docs/decisions/0031-myshop-brand-and-runtime-assets.md`.

Связанный этап: этап 21 — дизайн, шаблоны и статика.

### Факт по текущему проекту

В `src/prepare/` лежит исходный HTML/CSS шаблон с брендингом Hop & Barley. Проект, README и целевая сдача должны оставаться под названием `MyShop`.

### Конфликт

Если перенести шаблон без нормализации бренда, runtime UI, title, навигация, admin branding и документация начнут расходиться с названием проекта. Это ухудшит восприятие сдачи и создаст ощущение, что проект собран из чужого шаблона без адаптации.

### Принятое направление решения

Использовать Hop & Barley только как дизайн-концепт. Во всех runtime-шаблонах, README, документации, title, navigation, email-текстах и admin branding использовать `MyShop`.

### Что нужно сделать

- Заменить бренд в runtime UI на `MyShop`.
- Заменить бренд в admin UI на `MyShop`.
- Заменить бренд в title и alt-текстах.
- Не переносить англоязычные маркетинговые тексты без русской адаптации.
- Зафиксировать в README, что проект называется `MyShop`.

### Что изменилось по конфликту

Этап 21 закрыт: reference UI перенесён в runtime-шаблоны с брендом `MyShop`, русской локализацией, текущими Django URL/form contracts и без публичного упоминания Hop & Barley. Исходное название остаётся только историческим reference внутри `src/prepare`.

## C033. `src/prepare` как источник дизайна vs runtime-зависимость

Статус: решён.

Связанный ADR: `docs/decisions/0031-myshop-brand-and-runtime-assets.md`.

Связанный этап: этап 21 — дизайн, шаблоны и статика.

### Факт по текущему проекту

`src/prepare/` содержит подготовленные HTML-шаблоны, CSS, JS и изображения. Эта директория не является частью runtime-структуры Django-приложения и не должна требоваться для запуска проекта на чистой копии.

### Конфликт

Если runtime-шаблоны будут ссылаться на `src/prepare`, проект станет зависеть от вспомогательной директории. Это ломает clean clone, Docker build, `collectstatic` и production deployment.

### Принятое направление решения

Перенести нужную статику и Django-шаблоны в tracked runtime-директории. `src/prepare` оставить reference/source для дизайна и ассетов.

### Что нужно сделать

- Перенести публичные CSS/JS/images в `src/static/shop/`.
- Перенести admin CSS/JS/images в `src/static/admin_shop/`.
- Разнести Django-шаблоны по доменным директориям.
- Убедиться, что runtime templates используют `{% static %}`.
- Убедиться, что `collectstatic` собирает все нужные ассеты.
- Не импортировать и не читать `src/prepare` из runtime-кода.

### Что изменилось по конфликту

Этап 21 закрыт: CSS, JS и изображения перенесены в `src/static/shop`, runtime-шаблоны подключают их через `{% static %}`, подготовлен tracked-каталог `src/static/admin_shop`, а `collectstatic --dry-run --noinput --clear` проходит. JS адаптирован под реальную Django-логику и не переносит prototype-only `localStorage` авторизацию или клиентскую корзину.

## C034. Стандартная Django Admin vs современная удобная админка

Статус: решён.

Связанный ADR: `docs/decisions/0032-admin-ui-and-dashboard.md`.

Связанный этап: этап 23 — современная админка.

### Факт по текущему проекту

Текущая админка использует стандартный Django Admin с model admin настройками, filters, search fields, inline forms и actions. Новое требование просит сделать админку более современной, удобной и аналитической.

### Конфликт

Полная замена Django Admin на внешний пакет или отдельную кастомную панель может сломать текущие CRUD-сценарии, permissions, inline forms и уже реализованные admin actions. Но оставить стандартный UI без улучшений недостаточно для нового требования.

### Принятое направление решения

Сначала кастомизировать Django Admin и добавить staff-only dashboard, сохранив стандартные механики Django Admin. Не подключать внешний admin package на первом проходе.

### Что нужно сделать

- Сохранить `/admin/` как основной staff entrypoint.
- Добавить branding `MyShop`.
- Добавить современный dashboard.
- Улучшить product management визуально и навигационно.
- Сохранить model admin CRUD.
- Сохранить permissions.
- Сохранить inline forms.
- Сохранить admin actions.

## C035. Always-success mock payment vs weighted payment emulator

Статус: решён.

Связанный ADR: `docs/decisions/0033-payment-emulator.md`.

Связанный этап: этап 25 — Payment Emulator.

### Факт по текущему проекту

Checkout переведён с always-success mock на `apps.payment_emulator`. Эмулятор возвращает один из статусов `succeeded`, `failed`, `cancelled`, `pending` с дефолтными весами `7/1/1/1`.

### Конфликт

Новое требование просило отдельное приложение-эмулятор оплаты, которое возвращает случайный статус с весами: 7 успешных исходов из 10 и по 1 случаю на остальные статусы. Старая always-success логика не соответствовала этому требованию.

### Принятое направление решения

Выделить `apps.payment_emulator`. Модель `Payment` оставить в `payments`, а checkout перевести на результат эмулятора.

### Что изменилось по конфликту

- Добавлено `apps.payment_emulator`.
- Зафиксированы веса `succeeded=7`, `failed=1`, `cancelled=1`, `pending=1`.
- Random source можно подменять в тестах, поэтому проверки payment outcome детерминированы.
- Checkout различает successful и non-successful outcomes.
- При `succeeded` заказ оплачивается, остатки уменьшаются, корзина очищается.
- При `failed`, `cancelled` и `pending` заказ остаётся неоплаченным, остатки не уменьшаются, корзина сохраняется.
- Бизнес-документация и roadmap обновлены под фактическое поведение.

## C036. REST API slug routes vs URL table from ТЗ

Статус: решён.

Связанный ADR: `docs/decisions/0034-api-compatibility-routes.md`.

Связанный этап: этап 27 — REST API compatibility.

### Факт по текущему проекту

Product API сохраняет slug routes, например `GET /api/products/<slug>/` и `GET/POST /api/products/<slug>/reviews/`. Для соответствия таблице ТЗ добавлен compatibility route `GET /api/products/<int:id>/`.

### Конфликт

Если оставить только slug routes, формальное соответствие таблице ТЗ будет неполным. Если заменить slug routes на id routes, можно сломать уже существующие endpoint'ы, тесты и документацию.

### Принятое направление решения

Не ломать текущий API. Добавить compatibility routes по `id` и сохранить slug routes.

### Что изменилось по конфликту

- Существующие slug routes сохранены.
- Добавлен `GET /api/products/<int:id>/`.
- Добавлен `POST /api/users/login/` как alias JWT token obtain.
- Добавлен совместимый `GET/POST/PATCH/DELETE /api/cart/`.
- `GET/POST/PATCH/DELETE /api/cart/` переиспользует тот же cart service-layer, что и текущие `/api/cart/items/` endpoints.
- Swagger/OpenAPI и `docs/api.md` обновлены.

## C037. Local dev stand vs production HTTPS

Статус: решён.

Связанный ADR: `docs/decisions/0035-production-runtime.md`.

Связанный этап: этап 30 — production runtime.

### Факт по текущему проекту

Текущий Docker Compose запускает Django через `runserver` и PostgreSQL. Это удобно для локального запуска на текущей ПК, но не является production-ready runtime.

### Конфликт

Для сдачи нужно заложить закрытие сайта через Nginx, HTTPS и Gunicorn. При этом dev stand должен оставаться простым и запускаться локально без обязательных сертификатов.

### Принятое направление решения

Разделить dev и production сценарии. Dev остаётся простым, production использует Gunicorn, Nginx и Let's Encrypt/certbot.

### Что нужно сделать

- Оставить dev compose с `runserver`.
- Добавить production compose/profile.
- Добавить Gunicorn.
- Добавить Nginx reverse proxy.
- Добавить static/media volumes.
- Добавить `collectstatic`.
- Добавить HTTP to HTTPS redirect.
- Добавить Let's Encrypt/certbot script.
- Настроить secure production settings через env.

## C038. Англоязычные demo/template данные vs русскоязычная сдача

Статус: решён.

Связанный ADR: `docs/decisions/0036-russian-demo-data.md`.

Связанный этап: этап 22 — русскоязычные demo-данные.

### Факт по текущему проекту

Часть исходного шаблона и demo-товаров содержит английские названия и тексты. Сдача проекта должна выглядеть как русскоязычный интернет-магазин.

### Конфликт

Если оставить английские demo/template данные, UI будет выглядеть неоднородным и менее подготовленным. Если полностью русифицировать технические ключи, можно усложнить URL, тесты и стабильность seed-данных.

### Принятое направление решения

Runtime UI и seed demo data русифицировать. Технические slug/SKU/username/provider ids оставить ASCII.

### Что нужно сделать

- Русифицировать названия товаров.
- Русифицировать категории.
- Русифицировать описания.
- Русифицировать отзывы.
- Русифицировать имена покупателей.
- Русифицировать адреса.
- Русифицировать комментарии заказов.
- Оставить ASCII slug/SKU/username/provider ids.
- Обновить тесты seed-данных после реализации.

## C039. Admin analytics scope vs GraphQL analytics scope

Статус: решён.

Связанный ADR: `docs/decisions/0037-analytics-service-layer.md`.

Связанный этап: этапы 24 и 28 — админская аналитика и GraphQL аналитика.

### Факт по текущему проекту

Аналитика требуется в двух местах: современный admin dashboard и GraphQL endpoint `/graphql/`. Источники данных будут одни и те же: `Order`, `OrderItem`, `Payment`, `Product`, `Review`, `User`.

### Конфликт

Если реализовать расчёты отдельно в admin views и отдельно в GraphQL resolvers, логика быстро разойдётся. Метрики могут начать показывать разные значения, а тесты станут дублироваться.

### Принятое направление решения

Будущую реализацию аналитики строить через общий read/service слой, который используют admin dashboard и GraphQL resolvers.

### Что нужно сделать

- Создать общий слой аналитических selectors/services.
- Использовать его в admin dashboard.
- Использовать его в GraphQL resolvers.
- Покрыть общий слой тестами агрегатов.
- Покрыть admin и GraphQL только тестами доступа, формата ответа и smoke-сценариев.
